{
  "name": "@gr/interface-base",
  "version": "1.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "private": true,
  "scripts": {    
    "clean": "rm -rf dist",
    "build": "pnpm run clean && tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}