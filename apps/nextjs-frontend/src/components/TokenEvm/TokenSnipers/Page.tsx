import React
// { useState, useEffect, useRef } 
from "react";

// import { getApiChainId, getApiChainSimpleFromChainId } from "@/unit/web3";
// import { 
    // ITrans, ISplTokenTransProps, IApiResponse, IPairData, ITokenValue, 
    // ITokenCaProps } from "@/interface/iweb3";
// export default function Page() {
// export const EvmTokenSnipers = ({ pair, chainId }: ISplTokenTransProps) => {
    export const EvmTokenSnipers = (
        // { ca, chainId }: ITokenCaProps
    ) => {

    /* const [snipers, setSnipers] = useState([]);
    const [loading, setLoading] = useState(true);
    
    const [error, setError] = useState<string | Error | null>(null);
    

    const chainSimple = getApiChainSimpleFromChainId(chainId);
    const isSolana = chainId === "solana"; */

    return (
        <>
        evm/sol链api,获无法读取狙击手数据。先不实现
        </>
    )
}
