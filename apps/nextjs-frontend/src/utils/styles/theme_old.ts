// import {theme_const, TThemeColorScheme} from "@/config/theme_language_constants";

// import {TThemeSeries} from "@/config/theme_language_constants";
/* export function getThemeFromStorage(): string | null {
    if (typeof window === 'undefined') return null;
    return localStorage.getItem(ui_const.theme_key);
  } */

// 此函数，暂不启用．
/* export function getThemeSeries(theme: string): TThemeColorScheme {
    if (theme_const.dark_series_theme.includes(theme)) {
        return "dark";
    }
    if (theme_const.light_series_theme.includes(theme)) {
        return "light";
    }

    return theme_const.default_color_scheme;
} */

