/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    "@tailwindcss/postcss": {},  // ✅ 字符串形式使用
    // tailwindcss: {},
    // autoprefixer: {},
    // tailwindcss: {},
    // autoprefixer: {},
  },
};

export default config;
